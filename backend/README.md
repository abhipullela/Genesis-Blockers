run `pip install -r requirements.txt` to install all the requirements to run backend

app/
│
├── api/       → HTTP endpoints
├── models/    → data structures
├── services/  → application/business logic
├── config/    → configuration
└── main.py    → application entry point